 /// <reference types="cypress" />

 it ("test1",()=>{
    cy.visit("/");
    // cy.get(".sh").click(); // class
    // cy.get("*"); // all elements
    // cy.get(".more.button"); // 
    // cy.get("#search"); // id
    // cy.get("name"); // name
    // cy.get("plates, pento "); //all plates and all pento 
    // cy.get(".name1 name2"); sellect all name2 elements inside name1 
    // cy.get("plates *"); all elements in plates
    // cy.get("A + B "); SELECT  the element  directly follows A
    // cy.get("A~ b "); select all B follows A
    // cy.get("A>B"); SELECTALL B HAT ARE DIRECT CHILDREN OF A
    // CY.get ("plate orange:first-child") select all first child in plates
    // cy.get("plate apple:only-child");select the aplle that the only element in plate
    // cy.get("plate apple :last-child");
    // // cy.get("[attribute ]");






  });
